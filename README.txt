Install KUNA Python API via:

# for Python 2.7
pip install kuna

# for Python 3.x
pip3 install kuna

Decrypt command line arguments with command:

openssl des3 -salt -d -in CommandLineArguments.txt.encrypted
